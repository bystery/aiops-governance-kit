# AGENTS.md — <项目名> 项目宪法

> 给所有参与本项目的 AI 与人类。规则分"禁止/必须"两式，可机械检查。
> 违反任何一条硬性规则 = 改动作废。

## AI 协作模式与路由（每个 AI 会话先读这一节——用主控+子代理的项目必写，单会话项目删本节）

<参考写法：
- **主控（主会话）**：你是调度员不是工人——接活先读 `<方法论路径>/项目经验/主控卡.md`
  （会话入口+调度规程+汇报协议三合一），授权规则以 `<方法论路径>/项目经验/放权机制.md` 为准，
  并核对本项目 `.aiops/docs/PROGRESS.md` 头部"当前授权模式"行。
  **禁止亲自做大需求的活**：规划派 planner、编码派 worker、验收派 checker。
- **子代理**：读自己的投影 prompt（`.qoder/agents/`，由主控从
  `<方法论路径>/项目经验/智能体角色定义.md` 投影生成）——**不直接读角色定义**。
  投影缺失或版本过期 → 让主控重新生成。
- **需求分级**（拿不准就高不就低）：小=1 文件主控可自做 /
  中=2~5 五行任务单派 worker（六行字段枚举以主控卡§三为唯一权威：第 0 行主人原话引用 +
  改什么/依赖（无则写无）/最终状态清单/跑哪条快检/不碰什么/教训注入）/
  大=>5 文件或动分层或新增依赖或列不出清单 → 三角色流水线（planner 出 PLAYBOOK →
  按授权模式放行 → 分批 worker↔checker 循环 → 终验归档）。
防的坑：宪法是唯一保证被自动读的文件——路由不写在这里，主控就不知道要派活。>

## 代码质量红线区（worker 写码 + checker 验收共用的 rubric；项目可裁剪，方法论文件不复制第二份）

1. **先搜再写（DRY）**：动笔前 Grep 同类函数/常量/工具——仓库同能力实现只许有一份；
   发现相近实现，复用扩展，不许并行再造一份
2. **不做用不上的（YAGNI）**：不为"以后可能"加抽象/配置项/参数
3. **一件事一个函数**：函数 >80 行 / 文件 >500 行 = 要拆的信号
4. **最小改动面**：只动需求要求的，顺手重构走 backlog
5. **跟随项目已有风格和分层**
6. **失败路径必须有处理**（空值/失败/超时），不只写正常路径

## 项目一句话定位

<用一句话说清这个项目是什么、解决了什么问题、技术栈、规模。例如：
"XX 是一款 <类型> <技术栈> <规模>。">

## 架构总览（常驻活地图——本节永远反映当前架构，动结构必同步）

<画一个文字版的分层/模块依赖图 + 当前模块清单，标清依赖方向。示例：
```
ui（界面） → viewmodel（状态/调度） → domain（业务规则） → data（外部对接）
依赖只许向下。
```
当前模块清单：<一行一个模块/目录，新增文件归属哪个模块写清楚。示例：
- ui/：MainActivity、XxPanel…
- domain/：XxEngine、XxRegistry…
- data/：XxRepository…>

**本节纪律**：任何新增/移动/删除模块的改动，必须同步更新本节（可用门禁机械检查：
新文件路径未出现在本节清单 = 红）。要看更细的解剖图（数据流/依赖细节/新旧对照），
看最新归档：`.aiops/docs/archive/<最近大需求名>-大需求/ARCHITECTURE.md`。

---

## 硬性规则（违反即 FAIL）

### 分层
1. **禁止** <例如：UI 层直接读写 Repository/网络/文件——必须经 ViewModel>
2. **禁止** <例如：data/domain 层 import 任何 ui 类>
3. 新文件必须进对应分层目录，并同步登记到上方架构总览的模块清单
4. ...

### 安全与隐私（红线）
5. **禁止** 把 <敏感内容/密钥> 写入代码和日志
6. ...

### 错误处理与日志
7. 网络/文件操作必须有失败分支——不许只写正常路径（失败要能被看到、被处理，不许静默吞掉）
8. 网络请求必须设超时，不许无限等待
9. 关键路径（启动、核心功能链路）必须打日志，出问题时靠日志能定位
10. 日志禁止出现敏感信息（与红线第 5 条衔接）

### 变更纪律
11. 删除/移动文件前必须全项目搜索引用
12. 大需求执行期间，每批改动完成后必须：<构建/测试通过> + 过本需求 docs/manual-checklist.md 对应项
    （manual-checklist 是随大需求归档的清单；小/中需求不产它，用构建/运行验证把关）
13. 改动后同步更新 docs/；**动模块结构必须同步 AGENTS.md 架构总览**
14. 顺手优化一律禁止——记入 .aiops/docs/backlog.md
15. **新增第三方依赖必须落 ADR**（写用途、备选方案、许可证、官方来源链接），
    禁止为省一点事引入大框架——新增依赖 = 大需求（分级判据④，走大需求流程）
16. **每个新功能至少配一条能自动判定的行为测试**（启动得起来 / 核心动作结果对），
    测试命令进 `scripts/check.sh`

### 提交纪律
17. 一个 commit 一个意图；前缀用 `feat` / `fix` / `refactor` / `style` / `docs`
18. **禁止用 `git commit --no-verify` 绕过 pre-commit 钩子**——被钩子挡住 = 检查没过，
    先修问题再提交；绕过 = 门禁形同虚设
19. **大需求执行期间，PASS 后的整批 commit 必须打包**：本批代码 + checker verdict
    （.aiops/docs/audits/）+ PROGRESS 更新 + 本批产出的 ADR——过程文档躺在 git 未跟踪区
    = 防篡改检查空转、ADR 误删即失；提交他人追加的文件是义务，不是越权
20. **克隆后一次性执行 `git config core.hooksPath .aiops/scripts/hooks` 激活机械门禁三钩子**；
    钩子红 = 修问题，禁 `--no-verify`（三钩子职责与能力边界见 `.aiops/scripts/hooks/README.md`）

---

**过渡条款（存量违例处理）**：<如果有历史遗留违例写这里登记（文件+原因+治理计划），
大需求期间同步录入 docs/gate-waivers.md 走豁免；没有就删掉本节。>

## 新功能接入流程

1. 先列预计要动的文件清单，按大/中/小分级（判据见方法论第三章；拿不准就高不就低）；
   探索性/说不清验收标准的需求 → 走**原型通道**（红线全域生效 / 物理隔离（playground/ 整目录
   gitignore 或独立仓库）/ 转正必过 checker 体检，三条硬边界见方法论第三章）
2. 大需求：由 planner 先在 docs/ 写 PLAYBOOK（机械检查清单与判定标准开工前固化，checker 照单独立复核，
   角色定义见方法论《智能体角色定义.md》）；中/小需求直接进入下一步
3. 有技术取舍 → .aiops/docs/decisions/ 写一页设计登记（做什么/动哪些层/验证标准）
4. 实现代码（遵守分层）
5. <构建> + <测试>（大需求执行期间再 + 该大需求 manual-checklist 对应项）
6. **同步 AGENTS.md 架构总览与模块清单**（动结构时）

## 已知坑记录区（登记新坑请追加，勿删旧记录）

| 坑 | 症状/教训 | 状态 |
|---|---|---|
| <坑描述> | <发生了什么，怎么处理的> | ✅/待治理 |

## 文档地图

常驻（一直存在）：
- AGENTS.md（本文件，含架构总览活地图）
- .aiops/docs/PROGRESS.md — 进度现场（唯一进度依据）
- .aiops/docs/backlog.md — 待办登记 + 未决产品决策
- .aiops/docs/decisions/ — ADR 决策记录（永不归档）

归档（大需求做完封存，历史现场）：
- .aiops/docs/archive/<大需求名>-大需求/ — PLAYBOOK / MIGRATION_PLAN / ARCHITECTURE（深度快照）/
  ROADMAP（仅产品类需求）/ manual-checklist.md / audits/ / research/（调研报告，执行期在 docs/research/）/
  gate-waivers.md（清零后）
- 当前架构看本文件活地图；某次大改造的完整细节看对应归档的 ARCHITECTURE.md
