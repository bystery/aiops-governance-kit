# 增量迁移计划（大需求分步执行依据）

> **这是字段对照单，不是范文**——产出后自查字段齐全即可，文风/字数/详略自由。
> 每步独立可验证、可回滚。顺序原则：先契约与安全（P0），再治理（P1），后结构。
> 步内禁止夹带步外改动。
>
> **合并条款**：中小型大需求可把本计划**并入 PLAYBOOK 一份**（写成其独立一节，
> 每步仍用同一套 Ticket 字段），归档时只归一份，省一次维护同步；
> 大型重构 / 预计跨多轮会话的大需求仍单独成文。

---

## M1 <步骤名>（<给根因为P0/P1/结构>）
- **目标**：<这一步达成什么，一句话>
- **修改范围**：<精确到文件名：新建/修改哪些文件；预计 ~<行数> 行改动>
- **验收标准**：<锚定可观察行为——"点 X 应看到 Y"；引用附录 M 编号（如 M-通用 + M-12 + 冒烟）>
- **测试要求**：<worker 冒烟自测 1~2 条快检；引用附录 M"worker 自测项"编号——
  不抄命令，按需只读本步引用的检查项>
- **依赖**：<前置步骤（如 M1）；无依赖写"无">；**背景**：<本步对应 GOAL 哪条验收标准，一行；兼记改动为何等价、可回退>
- **不包含**：<本步明确不管什么（如：不顺手重构旧调用点）>
- **复用判定**：<本步动到的能力，仓库已有哪些相近实现（文件+函数名）→ 复用扩展 or
  确需新建 + 为什么——计划层不做复用判定 = 全链条完美执行出屎山>

## M2 <步骤名>
- **目标**：...
- **修改范围**：...
- **验收标准**：...
- **测试要求**：...
- **依赖**：...；**背景**：<对应 GOAL 哪条验收标准，一行>
- **不包含**：...
- **复用判定**：...

## M3 ...

---

## 合并与顺序约束（主控编排轮次的依据）

- **必须按序**：M1 → M2 → M3（<说明依赖关系>）
- **可合批**（≤3 步/批，checker 在台账内为各步留独立子节）：<Mx+My（同类、不动分层、不加依赖）>
- **绝不合批**：<动分层 / 新增依赖的步骤，为什么>

---

## 使用规则
- 每完成一步，把 Mx 前面打 ✅ 并记 commit 号
- 全部完成后进入终验阶段
