# <阶段号><审计类型>审计报告（<日期>）

> 只读审计产出，不修改任何文件。结论必须有代码证据（文件+行号）。
> 目的：信息收集，供后续阶段处置。
> **checker verdict 写法**：用作 verdict 时，判定**一次说清**——每条 rubric 一段
> "证据 + 结论"即可；**不写末尾汇总表、不写重复的夸奖/建议段**（同一内容说三遍
> 信息零增益，约多付 40% 行数）；记录用行式 `K: V`，不用表格（AI 读的文档用行式）。
> **批量验收 verdict 变体**：主控整批转交时（一批 ≤3 步），出**一份** verdict 代替
> 每步一份——文件名 `check-<批次号>-<日期>.md`，**每步一个子节**；verdict 头部复述转交单原文
> （路径\|事实\|期望vs实际，≤5 行）——转述可审计。格式：
>
> ```
> ## <步骤号> <步骤名>
> 结论：PASS / FAIL
> 证据：<机械检查结果（原样粘贴）+ 判定层证据（文件:行号 + 一句话）>
> ```

## 0. 审计范围与覆盖矩阵
<列出审计覆盖了哪些文件/范围，哪些跳过及原因。>

## 1. 审计摘要
<一句话总结项目健康程度，可给各维度打分（如架构/代码/安全/隐私/提示词等）。>

## 2. 问题总清单
| 级别 | 问题 | 文件:行号 | 描述 | 影响 | 建议处置 |
|---|---|---|---|---|---|
| P0 | <坏引用/隐私红线/Bug> | <file:line> | <描述> | <影响> | <修/搁置> |
| P1 | <...> | ... | ... | ... | ... |
| P2 | <小问题> | ... | ... | ... | ... |

## 3. 各专项结论
<按本次审计的专项（残留物/全面/错误/性能等）逐节给出结论。>

## 4 覆盖矩阵
| 文件 | 类型 | 状态 | 备注 |
|---|---|---|---|
| ... | ... | 已审/N-A | ... |

## 5. 给后续阶段的输入
<提炼出后续设计/迁移必须回应的强制输入清单。>

---

## 存档约定
- 审计报告命名：`<序号>-<类型>.md`（如 `00-baseline.md`、`10-debris.md`、`30-final-report.md`）
- 存到 `docs/audits/`
- 纪律：结论必须有证据（引用搜索命中数、文件存在性验证）；拿不准放"疑似"并给验证方法；不改任何文件
