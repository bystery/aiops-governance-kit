# 手动验证清单（大白话，<大需求名> 执行期文档——随需求归档）

> **落位**：执行期建在 `docs/manual-checklist.md`；大需求做完随该需求整体移入
> `docs/archive/<大需求名>-大需求/`，成为"这次改造验收了什么"的历史记录。
> 用项目主人能听懂的话写"怎样算正常"，不写技术黑话。不做也不阻塞任何阶段；
> 小/中需求不产它（用构建/运行验证把关）。

## 一、底线检查项（大需求开工时建立——每次改动后都该过的底线）

| # | 怎么验（大白话步骤） | 期望看到的正常结果 | 回填（✅/❌ + 日期） |
|---|---|---|---|
| 1 | <打开应用> | <能正常启动，不闪退> | <待验> |
| 2 | <核心功能一句话：…> | <…> | <待验> |
| 3 | <...> | <...> | <...> |

## 二、分步追加项（worker 每完成一步，把该步"人能看到"的验证项加进来）

### <步骤号 步骤名>（<日期>）

| # | 怎么验（大白话步骤） | 期望看到的正常结果 | 回填（✅/❌ + 日期） |
|---|---|---|---|
| 1 | <大白话操作步骤> | <期望看到的正常结果> | <待验> |

## 使用规则

1. 大需求开工时先填底线 3~5 条（能启动、核心链路能用、不闪退）
2. 每完成一个可提交的步骤，在追加区补几条"人能看到"的验证项
3. 只写"人能做/能看"的检查，不写依赖开发工具的黑话要求
4. **结果回填闭环**：主人亲手验完一项，AI 代为回填（✅/❌ + 日期）；
   AI 会话开头发现"回填"列有挂着"待验"的项 → 主动问主人一句"上次留的 N 项验了吗"，验完代填；
   回填 ❌ = 转修复流程（小需求/记 backlog）；一个需求的项**全部回填 ✅ 才算人工验收关闭**
5. 这份清单给项目主人事后抽查用，也用于每次改动后的手工回归
6. 归档后它是历史记录；下一个大需求另立新清单（各自独立，不跨需求累积）
