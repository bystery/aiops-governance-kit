# <项目名> <本次大需求名> 总控手册（三角色执行版）

> **这是字段对照单，不是范文**——产出后自查字段齐全即可，文风/字数/详略自由。
> 本文档是给 AI 看的总控指令，运行模式为【<全自动/半自动>】：
> <全自动：不许向用户提问题，一切决策自行做出并留档；半自动：关键决策点停下确认>。
> 用户是<描述用户水平，例如：第一次开发软件的初学者>，<看懂/看不懂代码>，无法做技术判断。
> 本文档生成于 <日期>。

> **审查机制**：worker 只冒烟自测、checker 异源独立重跑全量并落 verdict（权威见《智能体角色定义.md》）。

> **文档合并条款**：中小型大需求允许把 PLAYBOOK 与 MIGRATION_PLAN **合并为本文件一份**
> （PHASE 3 本身就是迁移计划的执行循环，两份重复度高，合并省一次维护同步）——合并时把分步计划
> 写成独立一节（每步仍含：动哪些文件 / 为什么安全 / 验证 / 改动量），归档时只归一份。
> 大型重构 / 预计跨多轮会话的大需求仍建议分两份。

## 修订表（放行前迭代记录）

> **二分规则**：**放行前**自由迭代，每改一版在本表加一行（不许越写越长的叙事段）；
> **放行后**本表冻结，任何改动必须走 ADR。
> 放行 = 按项目 PROGRESS 头部"当前授权模式"确定把关人（《放权机制.md》映射表）——
> 默认/基础放权下是主人，激进放权下主控代放行（自审+外查+记账）。
> 修订版把受影响步骤重写为终态，修订表只加一行指针（"vN：步骤 X 按终态重写，见 vN 文本"），
> 不在旧步骤上追加"但是/除外"式补丁。

- v1 <日期> 初稿（planner）
- v2 <日期> <一行：改了什么>（<谁指令：主人/自查>）

**主人原话引用**：每步 Ticket 增加一行"原话引用：需求登记第 N 条①栏逐字摘录"。

---

---

## GOAL（总目标）

<把本次大需求的目标用一两句话讲清楚，必须"可验收"——做完后怎样算成功。
例如：把 XX 从"AI 零散堆出来的项目"改造成"有地图、有宪法、分层清楚、小步演进"的可维护工程。>

GOAL 与验收标准必须能回指到登记账本③栏终态。

**硬性约束：**
1. 全程禁止推倒重写；任何时刻项目必须保持可<构建/运行>、核心功能可用
2. **执行与审查：** 每批改动后，由与 worker 不同源的 checker **独立重跑**附录 M 的
   checker 验收项，原始输出存进 checker 自己的 verdict 文件（docs/audits/check-*.md）——
   worker 不得代跑代贴，checker 不采信 worker 粘贴的历史输出（只作线索，不作证据）。
   门禁全部绿色=PASS；任何红色项=FAIL，必须修复后重跑该步全门检查。命中项属于
   <docs/gate-waivers.md> 已登记存量违例的，引用 waiver 编号豁免。**禁止跳过门禁、
   禁止伪造命令输出、禁止看到红色仍然判 PASS**
3. <全自动/半自动>连续执行：<全自动=不提问不等确认，一批接一批跑到底；唯一允许停下的情况=
   全部完成或出现无法自解的阻断（缺环境/要付费/要真实设备）>
4. 每完成一批必须做一次快照（git commit，工作区不干净时先 `git add -A && git commit`），
   并更新 <进度文档>（当前阶段/已完成/下一步/未决事项）
5. 会话上下文不够跑完全程时：把断点写入 <进度文档> 正常收尾，由下一个会话读它自动续跑
6. 自动回滚规则：某步修复 3 轮仍未通过 → **保证据回滚三步**：
   ① `git add -A && git commit -m "evidence: 回滚前现场（含 FAIL verdict 与进度）"`——现场全部入库；
   ② `git checkout <本批基线 sha（开工时记在运行计数行）> -- <文件清单>` 恢复上一批状态（本批新建文件用 `git rm`）；
   ③ `git commit -m "revert: 本批代码回滚，证据保留于上一提交"`。
   **禁止再单独跑 `git clean -fd`**——未跟踪的 FAIL verdict 会被删掉 = 销毁证据。
   该步记入 backlog 标记受阻，继续后续可独立执行的步骤

**启动前提（由用户保证，不要询问）：** 启动时没有其他 AI 会话在同时修改本项目。

---

## 主人须知（风险提示）——planner 开工前填，放行人必读

> planner 纪律"需求三问"的结果落在这里：这个需求**安全吗？涉及隐私吗？是公认的反模式吗**
> （比如"用 MD5 存密码"）？任何一问存疑 → 写在下面并给出 A/B 选项，**停下等确认，
> 不许默默照做**。无疑点也必须写一行"需求三问已做，无疑点"——这行是三问做过的证据，防跳过。
> **模糊需求的出口**：需求说不清验收标准/改哪 → 建议"先走原型通道澄清"
> （规则见方法论第三章），不许编造清单也不许干等。
>
> **放行纪律（防确认疲劳）**：送放行的材料（GOAL + 验收标准 + 本节 + 风险点节）
> 压缩到一页内，超了先砍。

<有疑点时逐条写：疑点是什么（白话）→ 可能的后果 → 建议的替代做法（A/B/C 选项）。
无疑点时本节只保留一行：需求三问已做，无疑点。>

## 不做什么（必含节——明确排除的范围，防蔓延）

<逐条列出本次需求**明确不做**的事。例如：不改旧版界面样式；不迁移历史数据；
不做性能优化（另立需求）；不引入新框架。说不清"不做什么"= 边界没想清楚，回去想。>

## 风险点（必含节——最可能出问题的三点，白话各一句）

<例：
1. 迁移用户数据时如果中断，可能出现新旧数据并存——已用"先备份再迁移"兜底；
2. 新缓存层首次上线可能拖慢启动——已加开关，出问题一键关闭；
3. 第三方接口限流可能让批量导入变慢——已改为分批+可断点续传。>

---

## 元规则（最高优先级，先读这里）

**规则1 关于用户（全自动模式）：** <禁止/允许>向用户提问；只有两种情况停下：全部完成、或规则内
无法自解的阻断。技术决策写 ADR（模板见附录 C）；偏好决策（产品体验/文案/默认开关）选保守、
可逆、贴近现有行为的默认值，ADR 中标注"用户可事后推翻"。用户参与后置细节见附录 D。

**规则2 关于审查机制（两层，不靠角色扮演、不靠自觉）：** 机械层=附录 M 清单（放行前定稿），
checker 每批独立重跑，任何红=FAIL（waiver 编号豁免，对应步骤完成时删该行）；判定层=rubric，
checker 读 diff 判定，结论引文件+行号，拿不准写"无法判定 + 还缺什么证据"，不许猜。

**规则3 关于证据：** 一切关于现状的结论必须给代码证据（文件路径 + 行号），禁止猜测。

**规则4 关于门禁：** 每个 PHASE 结尾有 PASS 条件，未达标必须返工，禁止带病进入下一阶段。

**规则5 关于改动范围：** 严格按本手册指定的范围改动。任何"顺手优化"都禁止——
想改的东西记入 `docs/backlog.md`，留给对应阶段处理。

**规则6 关于汇报纪律：** 输出给主人的内容按《主控卡.md》第七节白话四段式执行——术语自带解释、
代号必附白话；主人回"？"= 整条消息换白话重说。

**批依赖图**：每批一行依赖声明：`批1 ← 批2（批2 先行）`；无依赖关系的批标 `[同层可并行候选]`；同层判定 = 两批文件清单交集为空且无共享锚点文件。开闸前同层候选仍顺序执行。
**角色与流水线**：合批 ≤3 步/批，动分层/加依赖永不并批（轮数与阈值唯一权威见《智能体角色定义.md》
"轮次编排"节）；三角色权限速览见角色定义速览表，放行人见《放权机制.md》映射表。

---

## 流水线总览

<把本次大需求拆成几个 PHASE，画一个自上而下的流程，每阶段一句话说明目标。例如：>

```
PHASE 0 建立基线与安全网
   ↓
PHASE 1 大扫除/体检       [审计 → 处置 → 门禁复审 → 构建 → PASS]
   ↓
PHASE 2 架构重设计        [产出文档 → 门禁检查 → 修订至全绿]
   ↓
PHASE 3 增量执行          [循环：执行第N步→构建→门禁→全绿即commit→第N+1步]
   ↓
PHASE 4 终验与收尾        [门禁终验 → 对比基线量化改善 → 更新全部文档 → 归档]
```

> **PHASE 裁剪规则**：上面 PHASE 0~4 是**重建/重构类**大需求的完整形状，不是所有大需求
> 都长这样。按需求类型裁剪：**新增大功能类**可省 PHASE 1（大扫除）和 PHASE 2.1（逆向现状），
> 但保留 2.2 目标设计/2.3 分步计划；**纯治理类**可省 2.2。**PHASE 0（基线）与终验 PHASE 永不裁剪。**
> 裁剪了哪些 PHASE 必须在 PLAYBOOK 里写明理由，不许无声省略。

---

## PHASE 0：建立基线与安全网

1. 运行 `git status --short` 与 `git log --oneline -5`，记录当前状态。
2. 若工作区不干净：执行 `git add -A && git commit -m "chore: 重建前基线快照"`。
3. 生成项目文件清单，建立【审计覆盖矩阵】（登记口径：关键目录全量 + 其余按目录一行 + 抽样，
   模板见附录 B）。
4. 产出 `docs/audits/00-baseline.md`：文件清单、代码统计、当前已知问题。
   **首行必须写入 `BASELINE_COMMIT=<git rev-parse --short HEAD>`**（终验门禁要用它计算 commit 数）。
5. gate-waivers：**真有存量违例时**才建立 `docs/gate-waivers.md`，把 AGENTS.md 过渡条款登记的
   存量违例逐条录入；没有存量违例**不建空表**（空表也是仪式）——在 `00-baseline.md` 里记一行
   "无存量违例，未建 gate-waivers"即可。
6. **确认附录 M 的机械检查清单已逐条写死**（命令 + 期望输出 + 判读规则），
   grep 类命令各附一行双向校准记录（已知违例跑出红、干净基线绿——
   防"看着在检查、实际什么都没查到"的假尺子）。
   清单由 planner 在开工前固化；执行期间不许改写（要改 = 落 ADR 说明理由）。
   checker 后续照单独立重跑；主人也可让任何新会话重跑同一清单复核。

**PASS 条件：** 工作区干净、覆盖矩阵建立、基线文档落盘、附录 M 检查清单逐条定稿。

---

## PHASE 1：<大扫除/体检>

<写清本阶段的审计/处置步骤，每步给提示词模板引用（附录 A），并给门禁引用（附录 M-序号）。>
**PASS 条件：** 门禁全绿、构建通过、报告落盘。完成后 commit：`<commit message>`。

---

## PHASE 2：架构重设计（本阶段只产文档，不改任何业务代码）

### 2.1 逆向现状
通读全部源码与资产，产出【现状地图】（写给初学者）：功能清单（入口/状态）、端到端数据流、
模块依赖图（标出所有"乱线"）、病灶清单（按严重度排序，配代码证据）。
已有归档快照（最近一次大需求的 ARCHITECTURE.md）可作起点，但必须用源码核实后引用，禁止照抄。

### 2.2 设计目标架构
规模认知：<项目规模描述>。禁止过度设计。至少明确分层方案、每层职责边界（正反例）、
模块通信规则、资产/资源组织规范、状态管理规则。每条决策附白话"为什么"+放弃的备选方案，同步写 ADR。

### 2.3 制定增量迁移计划
拆成 N~N 个独立小步，按依赖排序。每步用 MIGRATION_PLAN 模板的 Ticket 字段
（目标 / 修改范围 / 验收标准 / 测试要求 / 依赖（无则写无；同行附**背景**：本步对应 GOAL 哪条
验收标准，一行）/ 不包含 / 复用判定）。
标出哪些步骤可合批（≤3 步/批）、哪些绝对不能合批（动分层/加依赖）。

### 2.4 更新项目宪法 AGENTS.md
AGENTS.md 开工时就已存在——本阶段**更新**它：架构总览（活地图）改为目标架构、
硬性规则按新架构修订、坑记录区追加本次逆向新发现的坑。

### 2.5 交付物
- `docs/ARCHITECTURE.md`：现状地图 + 目标架构 + 新旧对照（深度快照，做完随本需求归档）
- `docs/MIGRATION_PLAN.md`：分步迁移计划
- AGENTS.md 更新（含架构总览活地图）
- <产品类需求才要：`docs/ROADMAP.md` 决策菜单——纯技术重构不需要>

### 2.6 门禁检查
执行附录 M 对应门禁，检查交付物完整性与一致性。命令输出原样粘贴。全绿后 commit。

**PASS 条件：** 门禁全绿、交付物落盘。完成后 commit。

---

## PHASE 3：增量执行（核心循环，一批一步）

对 MIGRATION_PLAN 的第 N 批（一批 ≤3 步；动分层/加依赖的步骤单独成批；N = 1, 2, 3…）：
1. **工作区卫生检查（每批开工必做）**：跑 `git status --porcelain`，发现本批范围外的残留改动
   （上个会话/别处留下、未提交未处置）→ 停下报主控三选一（先提交 / stash / 丢弃），
   **禁止在残留上开工**——残留会混进 checker 的 diff 判定，越权与范围闭合全部失真
2. **复述留档**：本批做什么、动哪些文件、风险点——写入进度文档，不等待任何人
3. **执行**：worker 连做批内各步，只做清单内的事，禁止越权（发现的额外问题记入 backlog）
4. **冒烟自测（每步 1~2 条快检）**：只跑附录 M 对应项中标注"worker 自测项"的 1~2 条
   （能编译 / 核心路径能走通）；**禁止跑全量清单**——全量验收是 checker 的职责，
   跑了就是双倍浪费。失败则修复重试，同一步 2 轮仍败 → 带现场报主控（失败现象+已试方案）；
   3 轮 → 保证据回滚三步（见硬性约束第 6 条；禁止单独跑 `git clean -fd`——会销毁未跟踪的
   FAIL verdict），该步标记受阻记入 backlog
5. **汇报**：worker 按简明技术简报（结论+证据+下一步）向主控报告本批。**转交 checker 的
   信息只允许文件路径 + 客观事实，禁止夹带评价**
6. **复核**：checker 独立重跑本批 checker 验收项（附录 M 对应项全量）+ 按判定标准判读，
   出**一份批量 verdict**（每步一个子节：结论+证据+行号），落 `docs/audits/check-<批次>-<日期>.md`。
   任何红色/不通过 → 回给 worker 修复（≤2 轮，仍 FAIL 按主控卡第五节阶梯处置）。
   checker 验终局不验过程（逐条对最终状态清单）——步骤有误但终态全对 = 放行（偏差记
   backlog）；步骤全对但终态不对 = 打回。
7. **通过即提交并继续（DoD 四件套，缺一不许 commit）**：
   ①构建绿 ②测试绿（或替代冒烟绿）③检查清单绿（checker 批量 verdict 判 PASS）④技术简报已给。
   四件齐 → worker **整批 commit**（`refactor(migration-N): <批次名>`，**打包四样**：
   本批代码 + checker verdict 文件 + 本批 PROGRESS 更新 + 本批产出的 ADR
   （过程文档不提交 = 防篡改检查空转，"不改写内容"≠"不提交"），
   把大白话验证清单追加进本需求的 docs/manual-checklist.md，自动进入第 N+1 批

**PASS 条件：** MIGRATION_PLAN 全部步骤完成且每批都有 commit 记录。验终局不验过程：checker
只对最终状态清单判读——步骤有误但终态全对 = 放行（偏差记 backlog）；步骤全对但终态不对 = 打回。

---

## PHASE 4：终验与收尾
1. 执行终验门禁，与基线对比，量化改善。
2. 更新全部文档：ARCHITECTURE 定稿 / MIGRATION_PLAN 标记完成 / **AGENTS.md 架构总览同步为
   改造后的现状（活地图更新）** / 坑记录区 / 覆盖矩阵 / backlog。
3. 产出 `docs/audits/30-final-report.md`：改造前后对比、遗留问题、后续建议、全部 ADR 索引。
4. **waiver 清零检查**：gate-waivers.md 应为空（或剩余项已登记 backlog 并注明原因；
   本需求未建 gate-waivers 的——无存量违例，直接视为通过）。
5. **归档前置检查 3 条**（ADR 在位 / ROADMAP 未勾选决策已抄录 backlog / waiver 清零）
   → 通过后把过程文档（含 manual-checklist.md）移入 `docs/archive/<本次大需求名>-大需求/`。
6. 最终 commit。

**PASS 条件：** 终验报告落盘、活地图已同步、归档完成、全部文档与代码一致。

---

## 附录 A：审计提示词模板（用于只读审计阶段）

### A-1 审计员：<本次审计目标>审计

```
任务：<项目名> <审计方向>专项审计（只读，不修改任何文件）

你是接手项目的维护工程师。项目经多轮快速迭代，积累了大量残留。项目背景：<已了解的结构/位置/链路>。
已知起点线索：<顺藤摸瓜的线索，例如某个坏引用/某个要核实的方向>。

审计清单：
A. <类别>：<具体要查什么>
B. <类别>：<...>
C. ...

输出（结构化中文报告）：
1. 清理/问题总表：✅确认 / ⚠️疑似 / ❌误报
2. 每条：文件+行号、内容摘要、证据、建议处置
3. 坏引用即 Bug 清单（标 P0）
4. 收益/影响预估
5. 建议处理顺序
6. 覆盖矩阵（全部文件逐一状态，不许无声跳过）

纪律：结论必须有证据（引用搜索命中数、文件存在性验证）；拿不准放"疑似"并给验证方法；不改任何文件。
```

### A-2 说明
若已有对应的审计报告可直接复用，不重跑；需要更新时执行对应门禁命令即可。

---

## 附录 B：审计覆盖矩阵模板

| 文件/目录 | 类型 | PHASE1 状态 | PHASE2 状态 | 迁移后状态 | 备注（不适用原因等） |
|---|---|---|---|---|---|
| … | <类型> | 已审/清理/N-A | 已审/N-A | 未动/已迁移 | … |

**登记口径**：关键目录（如 src/ 核心代码区）逐文件登记；其余目录按目录一行登记；
大体量目录用抽样（planner 定抽样比例并写明抽了什么）。任何"N-A"必须写原因，禁止无声跳过。
全量逐文件登记仅在项目小到几十个文件时才值得——登记代价随项目变大线性上涨，
抽查 + checker 判读已够用。

## 附录 C：决策记录（ADR）模板

字段与使用规则（编号递增、只增不改、存 `docs/decisions/`）见
`项目模板/docs/decisions/ADR-模板.md.template`——权威在彼，不在此复制第二份。

## 附录 D：<全自动/半自动>模式说明

<写清用户参与后置到哪一步、安全靠什么兜底（门禁全绿 + 失败自动回滚）>
- 手动验证清单统一累积在 docs/manual-checklist.md（随本需求归档）；自动做出的决策全在
  ADR 里，用户事后可推翻；commit 自动执行，git log 可回看全程、任何时刻可回退；
  进度唯一依据是进度文档
- checker 的 verdict（docs/audits/check-*.md）是验收唯一依据；主人可让任何新会话重跑
  附录 M 同一清单核对与 verdict 存档一致（主人的"他律"复核手段）

---

## 缩写字典（可选节——项目自造缩写一次定义，全项目复用，任何会话不用再猜）

> 本项目用到的缩写/代号在此集中定义；新造一个缩写先来这里登记一行再使用。行式：

- 双绿：构建绿 + 测试绿
- M-x：附录 M 第 x 条门禁；W-x：gate-waivers 豁免编号；BL-x：backlog 编号
- <本项目自定义短代号及含义逐条列出，如"阶段4-步骤2"；C1~C6：……>

---

## 附录 M：机械检查清单（planner 开工前固化，checker 照单独立重跑）

> **设计原理**：预先写死的命令 + 期望输出——检查项开工前定死（验收标准不随结果漂移）、
> 同一命令谁跑都一样（主人随时可让任何新会话重跑复核）；checker 照单重跑+按标准判定，不临场自编。
>
> **两栏分档（防检查双跑）**：每步 PASS 条件清单拆两栏——
> ①**worker 自测项**：本步快检 1~2 条（能编译 / 核心路径能走通），worker 做完只跑这些；
> ②**checker 验收项**：全量清单 + 判定标准，只有 checker 跑。同一命令两栏都跑 = 双倍浪费。
>
> **内置占位：** M-测试 / M-密钥 / M-依赖 / M-地图 / M-通用 是模板内置项，planner 按项目
> 填具体命令后逐条写死，**不许无声删掉**（要删 = 落 ADR 说明理由）；M-发胖 为可选项；
> 分层 grep 等项目专属检查另加 M-<序号> 自定义项。
>
> **执行规则：** diff 类判据统一措辞："只应有追加（删除行须为重加行前缀）"，禁写"只应有插入"。
>
> - 清单随 PLAYBOOK **放行**而定稿（放行前自由迭代、修订表一行一版；
>   放行后禁止改写，要改 = 落 ADR 说明理由。放行人按《放权机制.md》当前授权模式定）
> - **每项标注运行节奏**：【每步】/【每阶段】/【终验】三选一，写在门禁名后面——
>   慢命令（全量测试、依赖漏洞扫描、全量构建）标"每阶段/终验"，不必每步全量重跑；
>   快命令（grep 类、diff 类）标"每步"。标注也由 planner 定稿，不许临场改
> - 每步由 checker 独立重跑对应项（按节奏跑当跑的项），**完整原始输出**存进 verdict 文件（docs/audits/check-*.md）
> - **每条命令必带超时**，构建/脚本类 ≤300 秒
> - 绿色 = 命中数 0（或符合预期值）；红色 = 命中数 > 0（或不符合预期值）
> - 全部绿色 = PASS；任何红色 = FAIL，必须修复后重跑全部门禁（不许只跑红的）
> - 禁止跳过任何一条、禁止伪造输出、禁止看到红色仍然判 PASS
> - **禁止改写脚本/命令**（尤其不许给管道加过滤来"减少命中"）；要改 = 落 ADR 说明理由
> - **定稿校准**：grep 类命令定稿前双向校准——造一个已知违例必须跑出红、
>   干净基线必须绿，校准记录一行（哪条命令、用什么违例验的红）；构建/测试类退出码自证，不用校准
> - **死尺子警报**：checker 发现某条命令跨多步永远零输出/输出一模一样 = 疑似失效，
>   提请复核（走 ADR），不许默默贴全绿
> - **Windows 环境守则：** 源文件多为 CRLF 行尾，grep 提取 → sort/comm 管道中途必须 `tr -d '\r'`；
>   grep 模式禁止用复杂字符类（如 `[/\\]`），一律用普通子串匹配；
>   构建命令先重定向到日志文件再取退出码（管道会吞非零退出码）
>
> **豁免机制（gate-waivers）：** 部分检查项存在存量违例，会让门禁"永远红"。
> 处理方式：建立 `docs/gate-waivers.md`。判读规则：候选命中 − waiver 已登记行 = 违规数，>0 才是红。
> 引用豁免必须写出编号；waiver 只减不增，治理完成时必须删除该行；终验要求清零。

### M-<序号>：<门禁名>【每步/每阶段/终验——planner 定】【worker 自测项 / checker 验收项】

```bash
# <检查项说明>
echo "--- <检查项> ---"
<grep/git 命令（不改写、不加过滤）>
```

**<门禁名> PASS 条件：** <明确写清哪些命中数必须为 0、哪些是可逐条判定的候选清单>
**分档：** <本条属 worker 自测项（快检）还是 checker 验收项（全量）；慢命令原则上归 checker 验收项>

### M-测试：行为回归门禁【每步】（凡项目有可运行测试集，必配此项——planner 纪律 5；
测试集大时可标"每步跑核心子集 + 每阶段全量"，子集清单 planner 定死，不许临场挑）

```bash
<测试命令，如 npm test / gradle test / python -m pytest> > test_gate.log 2>&1
echo "退出码: $? （0=成功，非0=失败）"
tail -5 test_gate.log
grep -c 'FAIL' test_gate.log
```

**M-测试 PASS 条件：** 退出码=0 **且** FAIL 计数=0（两个条件缺一不可）。
项目没有测试集时，planner 必须在此写明原因，并配至少一条替代断言（如启动冒烟、关键脚本校验）——
**禁止整份附录 M 只有 grep/git 类合规检查而没有一条行为类检查。**

### M-密钥：密钥泄漏门禁【每步】（内置项，每个大需求必配——两层都上：本条 grep 扫 + checker 判读）

```bash
# 常见密钥模式逐条扫（模式按项目语言增删，保持简单；有 gitleaks 时优先 gitleaks，没有用 grep 兜底）
echo "--- 密钥模式扫描：下面任何一条扫出实际内容行 = 候选，由 checker 判读 ---"
grep -rInE 'sk-[A-Za-z0-9]{16,}' --include='*.<源码扩展名>' <src 等源码目录> || echo "（无命中）"
grep -rn 'AKIA' --include='*.<源码扩展名>' <src 等源码目录> || echo "（无命中）"
grep -rn '-----BEGIN' --include='*.<源码扩展名>' <src 等源码目录> || echo "（无命中）"
grep -rInE 'api[_-]?key' --include='*.<源码扩展名>' <src 等源码目录> || echo "（无命中）"
```

**M-密钥 PASS 条件：** 真实密钥命中数 = 0，命中即红。候选行由 checker 读上下文判读
（区分真密钥与文档示例/占位符，判读写进 verdict）。确认真密钥 = FAIL 并立即提示主人
**撤销并更换该密钥**——密钥不设豁免登记（发现就该删换，不是登记了继续用）。

### M-依赖：依赖变更门禁【每步】（diff 类秒级；依赖漏洞扫描属慢命令，标【终验】另列或并入全检档；
内置项，凡项目有依赖清单 manifest/lockfile 必配）

```bash
# 本次改动是否动了依赖清单（manifest 与 lockfile 都要看）
echo "--- 依赖文件 diff ---"
git diff HEAD --name-only -- <manifest，如 package.json / requirements.txt / build.gradle> \
  <lockfile，如 package-lock.json / uv.lock / gradle.lockfile> || echo "（无）"
# 锁文件与清单一致性（按技术栈选一条，如 npm ci --dry-run / pip check）
<一致性检查命令>
```

**M-依赖 PASS 条件：**
1. 依赖文件有新增项而对应依赖**未在 ADR 登记**（用途 / 备选方案 / 许可证 / 官方来源链接）= 红；
2. 锁文件必须提交且与 manifest 一致（换台机器装出不同版本 = 红）；
3. checker 判定层核对 ADR 里登记的**官方来源链接真实存在**（防幻觉包/幻觉 API）；
4. 新增依赖的许可证类型由 checker 判读——GPL 类传染性许可证引入闭源项目 = 提示主人拍板；
5. （可选，planner 定阈值）依赖总数超上限 = 红（防依赖发胖，数 lockfile 顶层条目即可）。

### M-发胖：单文件行数上限【终验】（可选项，planner 定阈值——防单文件发胖）

```bash
# 列出最大的 5 个源文件；任一超过阈值（如 <800> 行）= 红（提示拆分：记 backlog 或落 ADR）
find <src 等源码目录> -name '*.<源码扩展名>' -exec wc -l {} + | sort -rn | head -5
```

**M-发胖 PASS 条件：** <写清阈值，如"最大文件 < 800 行；超限必须已在 backlog/ADR 登记拆分计划">。

### M-卫生：磁盘卫生列盘门禁【终验】（会话/项目收尾必跑——三条列盘命令各贴输出）

```bash
# 列物理文件对照预期清单，三条命令各贴输出
git status --porcelain
git status --ignored --porcelain   # .gitignore 豁免面
git clean -nd                       # 预览，不实删
```

**M-卫生 PASS 条件：** 未跟踪与被忽略文件逐个有归属，多出的无名文件 = 红；任何"已清理"结论必附当轮列盘输出。

### M-地图：架构总览同步检查【每步】（每次动模块结构的需求都要跑）

```bash
# 新增/移动的源文件必须出现在 AGENTS.md 架构总览的模块清单里
# 来源一：已提交/已暂存的新增文件
base=$(git merge-base HEAD 2>/dev/null || echo HEAD)   # 无基线分支时退化为 HEAD
echo "--- 来源一：git 新增文件（已暂存/已提交） ---"
git diff HEAD --name-only --diff-filter=A | grep '\.<扩展名>$' || echo "（无）"
# 来源二：尚未纳入 git 的新增文件（worker 漏 add 时这条会抓到——git diff 看不到它们）
echo "--- 来源二：未跟踪的新增文件 ---"
git status --porcelain | grep '^??' | grep '\.<扩展名>$' || echo "（无）"
echo "--- AGENTS.md 架构总览节中登记的模块/文件 ---"
sed -n '/## 架构总览/,/^## /p' AGENTS.md
# 判读：来源一或来源二出现的任何新增文件名未登记在总览清单 = 红（活地图过期）
# 注意：本检查跑在 worker commit 之前（工作区状态），两处来源都要看，缺一就是漏检
```

### M-通用：构建门禁【每步】（所有阶段共用，每次 commit 前必跑——"任何时刻必须可构建"是硬性约束，
不因构建慢降档；实在慢就在全检档外另配增量构建，由 planner 定死用哪条）

```bash
<构建命令> > build_gate.log 2>&1
echo "退出码: $? （0=成功，非0=失败）"
tail -3 build_gate.log
grep -c '<成功标志>' build_gate.log
```

**始终 PASS 条件：** 退出码=0 **且** grep 计数=1（两个条件缺一不可，防止管道吞退出码）。
