# <项目名> 架构文档（现状地图 + 目标架构 + 新旧对照）

> <阶段与日期> 产出 · 依据：全量源码通读 + 审计报告 + 现状证据。
> 所有现状结论均附文件:行号证据（规则3）。

---

## 第一部分：现状地图（写给初学者的"项目解剖图"）

### 1.1 这是什么
<用一句话+技术栈说明项目是什么、解决什么问题。>

### 1.2 功能清单（入口 / 状态）

| 功能 | 入口 | 状态 | 证据 |
|---|---|---|---|
| <功能> | <入口位置> | **完整**/半成品/废弃 | <文件:行号> |

### 1.3 端到端数据流
<画一个文字版的数据流，从入口到出口，标清每一步由哪个文件/函数处理，精确到行号。>

### 1.4 模块依赖图（箭头=依赖；⚠=乱线）
<画模块依赖图，标出所有"乱线"（依赖方向不对的）。>

### 1.5 病灶清单（按严重度）
| 级别 | 病灶 | 根因分类 |
|---|---|---|
| P0 | <问题> | <分类> |

---

## 第二部分：目标架构（克制版）

> 设计原则：<项目规模>。**不上**过度设计。只做"让乱线归位"的最小结构约束。

### 2.1 分层与职责边界
<画分层图，标清每层职责：什么必须放哪、什么禁止放哪。附正反例。>

### 2.2 <关键组件/资源>组织规范
<例如资产/配置/数据文件的唯一出口、命名目录规范，防止坏引用。>

### 2.3 模块通信规则
<何时构造注入、何时事件总线、何时禁止。>

### 2.4 状态管理规则
<UI 状态归属与单向数据流。>

### 2.5 数据层规范
<IO/并发/存储规范。>

### 2.6 测试策略（最小集）
<哪些最脆弱/最关键的地方需要测试兜底。>

### 2.7 新旧对照总表
| 维度 | 现状 | 目标 | 迁移步 |
|---|---|---|---|
| <维度> | <现状> | <目标> | <M数字> |

---

## 使用规则
- 现状部分随代码变更同步更新
- 功能清单/病灶清单是 ROADMAP 和 MIGRATION_PLAN 的依据
