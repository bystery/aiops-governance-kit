# <项目名> 项目进度（唯一进度依据，跨会话断点续跑现场）

> 当前授权模式：默认
> （换模式 = 改本行；这是授权状态的**唯一载体**，流程状态永不依赖会话记忆——见《放权机制.md》。
> 取值：默认 / 基础放权 / 激进放权）

> 依据 <.aiops/docs/PLAYBOOK.md，大需求执行期> 或 <日常任务> 执行。当前执行会话：<日期>。
> **三态分离**：同一信息只允许有一个家——计划态归 MIGRATION_PLAN（"打算做什么"），
> 本文件只持有**执行态**（"状态 + 指针 + 偏差"），证据归 verdict 文件。别把计划或命令输出抄进来。

## 当前阶段

<一句话：进行到哪。例如：**PHASE 3 增量执行，M7/M12 待开工**>

**给项目所有者的白话状态**：见 .aiops/所有者面板.md（唯一人文件）。

---

## 需求登记（P-01 五行制）

<每条需求先在账本占一节再流转，一节五行（字段语义见主控卡"需求五行登记制"）。样式示例：>

### 需求 <编号>：<需求名>

- **项目所有者原话**（逐字）：<项目所有者原话逐字粘贴，一字不改>
- **主控批注**：<查证事实、疑点、理解、推荐>
- **主控转述（终态书写）**：<一段"改完后世界"的描述（位置/对象/约束/可观察行为）>
- **项目所有者确认**：<原话确认或转述确认，未确认不进 PLAYBOOK>
- **交付回链**：<实现它的 commit 与 verdict>

---

## 已完成（单一区，按时间倒序；不设第二個"历史总览"区——双区=同一件事记两遍）

<只保留最近 2~3 项的详情（每项 2~3 行）；更早的一律压成一行**状态行**，格式：>

- 需求"导出功能" ✅ commit 789xyz（2026-08-20）
- M6 ✅ commit abc123（偏离：验收标准补了一条超时断言，见 ADR-12）
- M5 ✅ commit def456（偏离：无）
- M1~M4 ✅ commit 111aaa~444ddd（2026-08-17，详情靠 git log 找回）

---

## 下一步

<明确写清：按计划接下来做什么、依赖什么。新会话开头读本文件从这续跑。>

续跑入口：读 <文件>，从 <批号> 续

## 运行计数（大需求执行期，每批一行，随批 commit；行尾记本批用量量级，供成本对账）

<投影登记：投影（重）生成后记一行 `投影|版本|四份文件的 git hash-object 指纹`，供每轮开工双查核对>

<行式：`批号 | 阶段(done/failed/pending) | verdict 引用 | 待commit(y/n) | 备注`，字段用 ` | `
分隔，钩子按此解析；新格式行仍以 `- ` 开头（钩子3 的 `grep -c '^-'` 计数判据不变）。示例：>

- B3 | done | .aiops/docs/audits/check-B3-0822.md | y | 备注

## 未决事项

- <P0/P1 未完成的项>
- <被搁置/受阻的项及原因>
- <待办 backlog 里的关键项>

---

## checker 结论区（只由 checker 追加；worker 与 planner 禁止写入或删改）

<行式，一行一次验收，**两要素** `B<n> | verdict 文件路径`（PASS/FAIL 与证据在 verdict 文件内，登记行不复写）：

- B3 | 2026-08-22 | PASS | 证据：src/x.kt:12-40（示例行，登记时删除） | .aiops/docs/audits/check-B3-0822.md
- B2 | 2026-08-21 | FAIL | 机械检查红——M-密钥 1 命中（证据：src/y.kt:8）（示例行，登记时删除） | .aiops/docs/audits/check-B2-0821.md>

---

## 使用规则（给自己和后续 AI 看）

1. 本文件是进度的**唯一真相**，每次会话开始先读它，结束时更新它
2. 每完成一个可提交的步骤就更新一次"已完成"，不要攒到最后
3. **收尾先压缩再更新**：每次会话收尾，先把"已完成"区老条目压成状态行
   （只留最近 2~3 项详情），再写新内容——压缩是收尾的固定动作，不是可选项；
   快检有兜底黄牌：`wc -l .aiops/docs/PROGRESS.md` 超过 <120> 行 = 黄牌，提示该压了
4. 会话中断/上下文不够 → 把断点写进"下一步"再收尾，下次照此续跑
5. 执行产物（命令输出、检查结果）**不粘进本文件**——证据归 verdict/audits，这里只记状态与指针
6. checker 结论区只由 checker 追加——执行记录与验收结论分离，worker 不得代填
