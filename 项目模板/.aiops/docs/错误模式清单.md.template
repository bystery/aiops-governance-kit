# 错误模式清单（教训注入源——机器可读，主控派单前机械核对）

> 本文件为常驻文档第 5 类（项目内实例化存 `.aiops/docs/错误模式清单.md`）。
> 用途：把主人纠过的错变成派单时机械注入的教训，防同一教训跨会话重犯。

## 登记

"阶段"列取值 = 对齐/计划/执行/验收/汇报。

| 阶段 | 模式串 | 正确写法 | 来源（BL/I-xx） | 登记日期 |
|---|---|---|---|---|
| 执行 | 兑底 | 兜底 | I-22 | 2026-08-30 |
| 验收 | .gitignore 豁免面不进 git status | 列盘必带 `--ignored` | I-13 | 2026-08-30 |

---

## 使用规则

1. 主人纠正当日落盘一行，否则视为未处理
2. 主控派单前按任务单所属阶段过滤：提取登记表'模式串'列中阶段=本单阶段的行，`grep -F -f <提取文件> <任务单文本>`，命中行机械拼进'教训注入'段；跨阶段命中不注入（直接对整表跑 grep -F -f 不作数）

3. 复发升档：checker 在批量 verdict 中判 FAIL 项是否命中清单既有模式串——命中 = 复发，verdict 记一行 `复发|模式串|批号`；主控见复发行把 backlog 对应条目升档（上移或标注"升档"）并记 PROGRESS 运行计数。判定人 = checker，登记处 = verdict + PROGRESS。
