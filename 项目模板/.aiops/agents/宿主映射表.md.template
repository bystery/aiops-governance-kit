# 宿主映射表（投影正本永在 .aiops/agents/；宿主侧只是渲染结果，加宿主 = 加一行）
> 正本列在主控首次生成投影前为占位；渲染方式 = 复制条文段，frontmatter（model/tools）归宿主保留。

| 角色 | 正本 | 宿主渲染目标（按项目宿主填，加宿主 = 加一行） | 渲染方式 |
|---|---|---|---|
| planner | .aiops/agents/planner.md | <宿主代理目录>/planner.md（示例：Qoder = .qoder/agents/planner.md） | 复制条文段 |
| worker | .aiops/agents/worker.md | <宿主代理目录>/worker.md | 复制条文段 |
| checker | .aiops/agents/checker.md | <宿主代理目录>/checker.md | 复制条文段 |
| researcher | .aiops/agents/researcher.md | <宿主代理目录>/researcher.md | 复制条文段 |

落盘布局：.aiops/docs = 过程文档 · .aiops/scripts/hooks = 机械门禁（`git config core.hooksPath .aiops/scripts/hooks`）· .aiops/所有者面板.md = 唯一人文件 · .aiops/读集.md = 读集边界。
