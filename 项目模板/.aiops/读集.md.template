# 读集（常读/按需边界——唯一清单；净效果=常读不涨，新增读什么必须指出删什么）
## 常读集（每会话必读，前缀稳定区，顺序固定）
1. 根 AGENTS.md（指针 → .aiops/AGENTS.md）
2. .aiops/AGENTS.md（宪法）
3. .aiops/docs/PROGRESS.md（进度现场 + 授权模式行）
## 按需集（触发才读，插入后顺序不再变动）

### 按需取用表（要产出什么 → 读什么；示例仅当模板字段不明时读一份）
| 要产出 / 动作 | 模板 | 示例（填好版，仅当模板字段不明时读） |
|---|---|---|
| 新项目初始化 | kit 仓侧一次性动作（在框架仓运行 `项目模板/scripts/init-project.sh <目标目录> <项目名>`），本文件生成后此行在目标项目内不再使用 | — |
| 记进度 | `.aiops/docs/PROGRESS.md`（字段与使用规则见文件尾"使用规则"节） | — |
| 登记待办 | `.aiops/docs/backlog.md`（字段与使用规则见文件尾"使用规则"节） | — |
| 技术决策 ADR | `.aiops/docs/decisions/ADR-模板.md`（字段与使用规则见文件尾"使用规则"节） | 〈例〉`.aiops/docs/decisions/ADR-1-<决策名>.md` |
| 大需求开工 | 〈归〉`.aiops/docs/archive/归档件模板/PLAYBOOK.md.template` | 〈例〉`.aiops/docs/archive/<大需求名>-大需求/AI_REBUILD_PLAYBOOK.md` |
| 分步迁移计划 | 〈归〉`.aiops/docs/archive/归档件模板/MIGRATION_PLAN.md.template` | 同上目录 `MIGRATION_PLAN.md` |
| 架构快照 | 〈归〉`.aiops/docs/archive/归档件模板/ARCHITECTURE.md.template` | 同上目录 `ARCHITECTURE.md` |
| 产品路线图 | 〈归〉`.aiops/docs/archive/归档件模板/ROADMAP.md.template` | 同上目录 `ROADMAP.md` |
| 人工验收清单 | 〈归〉`.aiops/docs/archive/归档件模板/manual-checklist.md.template` | 同上目录 `manual-checklist.md` |
| 审计报告 / verdict | 〈归〉`.aiops/docs/archive/归档件模板/审计报告模板.md.template` | 同上目录 `audits/30-final-report.md` |
| 归档收尾 | `.aiops/docs/archive/归档说明.md`（规则在文件内） | — |
| 错误模式清单 / 登记教训 | `.aiops/docs/错误模式清单.md`（字段与使用规则见文件内"使用规则"节） | — |
| 需求对齐拷问（grilling） | .aiops/skills/grilling（来源写死：install-grill-skill.sh 下载 mattpocock/skills 钉死 commit） | — |

- .aiops/docs/错误模式清单.md——触发：派单前（按阶段过滤注入）
- .aiops/docs/archive/ 归档件模板——触发：要产出对应文档那一刻读模板；示例可取本仓库已归档的历史文档
- .aiops/docs/decisions/——触发：考古决策依据
- .aiops/agents/宿主映射表.md——触发：生成投影/派子代理前
- .aiops/docs/research/ 调研报告——触发：采信前抽验出处；执行期在手 ≤3，终验清零
- .aiops/skills/ grilling 技能——触发：需求对齐拷问前，check-grill-skill.sh 绿才开工
