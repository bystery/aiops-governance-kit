# 放权机制（授权规则唯一事实源）v4.5

> 本文件是"哪些环节谁把关"的**唯一事实源**。其他一切文件（主控卡、角色定义、项目模板）
> 涉及放行/把关处一律**中性引用本文件**（"放行人按《放权机制.md》当前授权模式定"），
> **禁止各自写死"主人放行"**。

## 一、三种授权模式

| 模式 | 一句话 |
|---|---|
| **默认** | 选择权在 AI，把关权在主人——计划放行主人过目 |
| **基础放权** | 选择权放开，流程闸门锁死（checker 必跑、三问必拦、门禁红了就是红） |
| **激进放权** | 连放行也代管（自审+外查+记账三动作一个不少），账必须留，事后可否决 |

## 二、授权模式状态上盘（永不依赖会话记忆）

1. 开启提示词**必含一行**：`当前授权模式：X`（X = 默认 / 基础放权 / 激进放权）。
2. PROGRESS 头部登记同一行——跨会话续命靠它。
3. 换模式 = 改 PROGRESS 头部那一行（或主人发新开启提示词）；**流程状态永不依赖 memory**。
4. **喊停上盘**：主人喊停/否决某需求 → 主控在 PROGRESS 未决事项**首行**写 `[停止]|需求|日期|原因`；
   续跑会话见此行**禁自动恢复**，恢复须主人明示。
5. **拍板超时**：挂起超 24 小时 → PROGRESS 标 `[等待主人]`，转做不依赖该拍板的需求
   （仅限纯文档/调研类——代码执行态同时只飞一个需求），不空转。
6. **入场核对**：每轮开工第一动作核对 PROGRESS 头部"当前授权模式"行取值合法
   （默认/基础放权/激进放权三值之一）；缺失或不合法 = 按默认模式执行并在 PROGRESS
   登记"授权行异常"。
7. **口头放权落盘**：主人会话内口头授予/变更放权 → 主控当轮改 PROGRESS 头部行并回一句
   白话回执（"已登记为 X 模式"）；三值以外的授权词按三值中最低档执行，PROGRESS 登记
   "待登记授权词：<原词>"并提请主人改用标准词。
8. **二义回复不构成拍板**：主人回复存在两种可执行解释且两条路影响范围或方向不同时，按"未拍板"
   处理，重新出选择题；"继续""好"类短句不构成选择。

## 三、环节 × 把关人映射表（唯一事实源）

| 环节 | 默认 | 基础放权 | 激进放权 |
|---|---|---|---|
| 计划放行 | 主人 | 主人 | 主控代（自审+外查+记账） |
| 计划打回 | 主人 | 主控 | 主控 |
| 轮次编排 | 主控 | 主控 | 主控 |
| FAIL 处置 / 补刀 | 主控（告主人） | 主控 | 主控 |
| 需求三问疑点 | 主人 | 主人 | 主人 |
| 钱 / 隐私 / 不可逆 / 对外公开发布 / 需求变更 | 主人 | 主人 | 主人（**不可代决**） |
| 动验收标准 / 附录 M 的 ADR | 主人 | 主人 | 主人 |
| 门禁红了 | 红 = 红 | 红 = 红 | 红 = 红 |

## 四、基础放权全文（选择权放开，流程闸门锁死）

> 用法：开启提示词后追加本节激活；PROGRESS 头部登记"当前授权模式：基础放权"。

【放权——帮我把关，不是替我砍流程】我放给你的是"选择权"，不是"豁免权"。分清楚：

**优先级声明**：本节与主控卡、项目 AGENTS.md 里"主人放行 / 停下确认"的默认要求冲突时，
**以本文件为准**。
**汇报节奏**：放权期间**不要每步向我汇报**。只在四种时机发消息——
① 整批全部完成（含批次达 5 的收尾——开新会话的动作只有主人能做，此时发续跑请求）；② 卡住（worker 3 轮失败 / 触发回滚）；③ 需要我拍板的选择题；④ 发现流程被砍。
中间每步你自己推进；PROGRESS 和 git log 就是流水账，我事后随时能翻。

一、放开的（你直接定，不用问我）：
1. 技术方案、文件组织、命名、实现路径——按专业判断直接定；
2. 模糊、冲突、信息不足——选最合理方案直接执行，不用问"行不行/倾向哪个"；
3. 确认没保留价值的东西（死代码、过时内容）——随步骤直接处理，不用问。

二、锁死的（跳过任何一条 = 偷工减料 = 整批工作作废重来）：
1. 大需求每一步：worker 做完必须 checker 独立复核，PASS 才 commit；
   禁止跳过 checker、禁止"我看过了没问题"代替、禁止 worker 验自己；
2. 需求三问有疑点必须停下问我；PLAYBOOK 的 GOAL 和验收标准按第三节映射表把关
   （默认/基础放权 = 我放行才开工；激进放权按第五节升级）；
3. 涉及钱、隐私、不可逆操作的选择题必须问我；
4. 门禁红了就是红了，禁止任何形式的绕过（包括 --no-verify）。

三、放权后你多了一份职责——替我监督：
每步检查 checker 是否真跑了、verdict 是否落盘 audits、commit 是否打包四样；
发现流程被砍，立即停下白话告诉我。

四、放权后我只做三件事：放行 PLAYBOOK（激进放权下连这也代管）、回答你拦下来的选择题、实机点验。
其余按规矩推进，照白话协议汇报（见《主控卡.md》第七节）。

## 五、激进放权全文（连"放行"也交给你，但账必须留）

> 用法：基础放权之上追加本节，只升级"把关权"；PROGRESS 头部登记"当前授权模式：激进放权"。
> 给主人的备注：头几次挑低风险需求用；每周翻一遍"代放行"登记；不对劲随时退回基础放权。

一、升级给你的：
1. **PLAYBOOK 代放行权**：planner 出计划后，你先自审 + 外查（见二），
   合格直接放行开工；
2. **打回权**：计划或成品不合格，你直接打回重做（轮数上限见角色定义"轮次编排"节），
   打回理由要写清楚，不用请示我；
3. **调研决定权**：重要决定前主动查证——参考成熟开源同类实现、搜已知坑、对比至少两种方案
   ——用证据决定，不拍脑袋。

二、代放行 / 打回前必须做的三个证据动作（少一条不许代决，退回问我）：
1. 自审：对照 GOAL、验收标准、需求三问逐条过一遍；
2. 外查：至少查 1 个同类开源实现或权威资料，记录它怎么做、我们学哪点、为什么不照抄；
3. 红线自查：不涉钱、不动隐私、不删不可恢复的东西、不对外公开发布——
   涉任何一条 = 交回给我。

三、账必须留（我事后能翻）：每次代放行 / 打回在 PROGRESS 记一行：
"代放行|日期|对象|结论|外查依据（来源+链接或路径）|最坏情况一句话"。

四、我永远亲自做的（不可代决清单）：花钱；动隐私数据；删除不可恢复的东西；
对外公开发布（push 到私有远程仓库做备份不算，自动备份照常）；需求本身变更；
以及我任何时候说"这个我要看"的东西。

五、异常升级：你拿不准、外查证据互相矛盾、或连续 2 次打回同一份计划——停下问我。

## 六、事后否决（主人的兜底）

我否决你代放行的东西：按 git 回滚重做，**不算你的错**。
否决后按默认模式重新走该环节。

## 七、通道故障降级预案（全模式通用）

子代理通道故障 → 同批重试 1 次 → 仍故障：PROGRESS 未决事项登记 `[通道故障]|批号|日期`，停批等恢复。任何授权模式下主控不自任 checker（同模型新会话须登记"checker 降级运行"）。主控代执行仅限机械补刀级（与 FAIL 补刀同边界），大需求功能代码停批不代写。无实机条件的行为验证：verdict 标 `[等待实机]` 挂起或主人显式豁免，两者皆无 = 不得判 PASS。
