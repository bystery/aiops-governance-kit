# 需求书：researcher 机制改造 + 文件治理

> 来源：用户在 aiops-governance-kit 项目讨论中提出的全部需求与意见。
> 每条需求三块：**用户原话**（逐字存档，永不改动）／**自己尝试理解**（标注"不一定正确"，
> 如有出入以用户原话为准）／**验收判据 + 边界与约束**（可验证的结果与边界，2026-09-04 评审后补）。
> 优先级、拍板决议与全局非功能约束见文首"优先级、拍板决议与全局约束"节。
> 本文件仍不含实现方案——怎么改文件、怎么写脚本一律归《实施指导书-researcher机制与文件治理.md》。

---

## 优先级、拍板决议与全局约束（2026-09-04 评审后补录）

### 优先级

| 序 | 需求 | 理由 |
|---|---|---|
| P0 | 需求5 文件爆炸 | 有实测翻车基线（134 文件），浪费 token 最直接 |
| P0 | 需求1 grill-me 独立技能 | 需求对齐质量是全链条上游，问不痛则后面全白做 |
| P1 | 需求2 / 需求3 分流路由与参考式拷问 | 提升对齐质量，但依赖需求1 的技能到位 |
| P1 | 需求4 子集文件控制 | 需求2 的配套纪律，随需求2 落地 |
| P2 | 需求6 文件归一处 | 现有 `.aiops/` 布局已基本达标，只缺机械门禁 |
| P2 | 需求7 交付回链时序 | 与现行机制同向，属补门禁 |

### 项目所有者拍板决议（8 项）

1. **多宿主通用**：条文与机制描述必须宿主无关，禁止写死某一个宿主的目录名。
2. **调研者与人的通信通道 = 主控逐字转发**：调研者 → 主控 → 项目所有者 → 主控 → 调研者。不开"子代理直接对项目所有者说话"的例外。
3. **联网是硬前提**：明确不支持无网/内网环境使用；因此"技能写死从网上下载、缺失即硬红"成立，不做离线兜底副本。
4. **第三方技能许可**：`mattpocock/skills` 为 **MIT**（`Copyright (c) 2026 Matt Pocock`，2026-09-04 取回 LICENSE 全文核实）——运行时下载不构成再分发，须署名；MIT 亦允许将来改为内置副本。
5. **临时件不计入文件预算**：用完即销的文件（需求登记子集、调研报告）不占活跃预算；ADR 不设上限。
6. **临时件先入库留证再删除**：不许"从未入库直接删掉"（销毁证据）。
7. **需求7 要机械回填门禁**，不只是口头强调。
8. **净减尺子按"删旧抵消 + 记账"**：机制扩展允许净增，须落 ADR 如实记账。

### 全局非功能需求

| 项 | 要求 |
|---|---|
| 机械性 | 需求1/4/5/6/7 的判据必须由**钩子级机械门禁**承载；凡"靠模型自觉执行"的门禁视同未实现（这正是需求1 抱怨的根因） |
| 通用性 | 落盘路径与加载机制一律用中性描述 + 宿主映射，禁止在模板中硬写具体宿主目录 |
| 中文路径兼容 | 所有机械判据对中文文件名必须有效（本项目治理文件名以中文为主） |
| 证据不销毁 | 任何"删除中间产物"的规则，删除前必须已入库留证 |
| 联网 | 需求对齐拷问依赖联网下载技能；不支持离线（拍板 3） |
| 可维护性 | 新增门禁不得与现行铁律冲突（尤其铁律6"ADR 永远留在 decisions/"）；冲突即视为设计缺陷，不得靠人工豁免度日 |

### 存量兼容口径

- 新门禁只对**新增批次**生效，不追溯历史：老项目 `audits/check-<批次>-<日期>.md` 保留原样，不强制迁移为台账。
- 存量 `.aiops/docs/` 已超预算的项目：首次触红时归档一次即可，不倒查历史 commit。

---

## 需求1：grill-me 必须独立成 skill，且写死从网上下载

**1. 用户原话**

> 1.grill-me 是内含的，不是独立 skill ！！问题所在，我说呢 怎么感觉ai给我提的问题都是不痛不痒的！而且有时候根本不会grill me！

> 6. 不行！ 必须写死 从网上下载grill me skills

**2. 自己尝试理解（不一定正确）**

用户认为当前 grill-me 只是主控卡里的一句话纪律、靠模型自觉，这是它失效（问题不痛不痒、有时根本不问）的根因。用户的诉求是：grill-me 不能再以"内含一句话"的形式存在，必须变成一个独立的、可下载的 skill，并且在项目里有"写死"的机制确保主控一定会去下载/加载它。不是可选项，是硬要求。

**3. 验收判据（可验证）**

拆成两组，两组都过才算达标。

*1a 拷问质量（抽查最近 3 次需求对齐记录）*

| 判据 | 达标 | 不达标 |
|---|---|---|
| 分轮提问 | 一轮把当前 frontier 的问题一次问完并编号 | 一次只问一两个、来回挤牙膏 |
| 每问带推荐答案 | 每个编号问题后附"我的推荐答案" | 只抛问题不给推荐 |
| 结束条件 | frontier 问空才结束 | 问两三个就自行开工 |
| 有效性 | 记录里至少有一次追问导致项目所有者改口/补充约束 | 全程无人改口（= 问题不痛不痒） |

四项缺一即"未达标"。

*1b 技能到位机制（每次开工前机械核对）*

- 来源写死为 `mattpocock/skills`，钉死 commit `3cca18b368ae95cdbdebbff572ccafa662551015`；
- 双指纹核对：`grilling/SKILL.md` = `8ca78c6d8f901aab0c5a1f896034b70e666ff2a3`（1987 B）、
  `grill-me/SKILL.md` = `3947ff9c4ad980d14fc07fccbf659d47c114e81d`（157 B）；
- **`grilling` 缺失或指纹不符 = 硬红，禁止进入需求对齐**（它承载全部方法论）；
- `grill-me` 缺失只警告不拦（它是 7 个词的转发壳，且带 `disable-model-invocation: true`，模型本就无法自动调它——这恰好解释了原话里"有时候根本不会 grill me"）。

**4. 边界与约束**

- 判据 1b 必须由脚本执行，写进"主控自查表"不算落地（见全局非功能需求·机械性）；
- 依赖联网（拍板 3），无网即硬红，不做离线兜底副本；
- 署名义务：MIT 要求保留 `Copyright (c) 2026 Matt Pocock`（拍板 4）。

---

## 需求2：researcher 接管需求对齐——路由与流程

**1. 用户原话**

> 1.用户需求->主控判断，哪些是中小需求，需求明确的，直接写入五行登记制.md，只有不明白的 才交给research（特例可以和用户交互！并放宽权限），得到五行登记子集.md，然后没有矛盾才能给 主控，主控进行合并（并检查是否有矛盾）；而且因为是五行登记所以不需要 考虑转述变味的问题

> （第一轮补充背景）然后 由research 给出需求五行登记制.md文件，然后自己检查一遍是否有矛盾（就是前后需求不一致的有冲突的一定要问清楚），没矛盾再给主控，主控自己再检查一遍若没有矛盾，这个需求对齐 阶段才算正式完成，然后再开始playbook 啥的 planner啥的

**2. 自己尝试理解（不一定正确）**

需求对齐阶段的路由改成分流制：用户需求先到主控，主控判断——如果需求明确（中小需求），主控自己直接写进五行登记制，不经过 researcher；只有需求说不明白的，才交给 researcher。researcher 处理完后产出"五行登记子集.md"，先自己检查有没有前后矛盾（前后不一致、有冲突的必须问清楚），没矛盾才交给主控。主控拿到子集后做合并，并再检查一遍有没有矛盾。双重矛盾检查都过了，需求对齐阶段才算正式完成，之后才能进 playbook / planner。

用户认为：因为全程用的是五行登记格式（原话逐字入账），所以不存在"转述变味"的问题——这等于用五行登记制本身替代了原来的"转述忠实度"防线。

researcher 在处理这类需求时是"特例"，可以和用户直接交互，并且权限要放宽（区别于现在 researcher"不直接对项目所有者说话、不做决定"的边界）。

**3. 验收判据（可验证）**

- **分流决定必须留痕**：每条需求在账本里带一行 `分流=主控直办 | 调研者对齐特例`，缺此行 = 未落地（否则"主控判断"无法事后核对，等于没分流）；
- **子集文件闭环可查**：走特例的需求，其子集文件必须在 git 历史里查得到（留证），且工作区已删除（见需求4）；
- **双重矛盾检查各留一行**：researcher 与主控各记一行 `矛盾复检 | 日期 | 比对节数=N | 冲突=无 / 已问清(节号)`；两行缺任一 = 需求对齐阶段未完成，不许进 PLAYBOOK / planner；
- **时序判据**：`矛盾复检` 两行的时间必须早于 PLAYBOOK 首次落盘。

**4. 边界与约束**

- **通信通道 = 主控逐字转发**（拍板 2）：调研者 → 主控 → 项目所有者 → 主控 → 调研者。原话里的"特例可以和用户交互"按"经主控逐字转发实现交互"落地，**不开子代理直连的口子**——直连会与"主控是唯一发言人"和"子代理不直接对项目所有者说话"两条现行铁律正面冲突，而现行冲突处理条款要求按更严格一份执行，等于该例外永远无法使用；
- 逐字转发的纪律要求：主控不得改写、概括、增删调研者的问题与项目所有者的回答；
- 特例仅在**需求对齐阶段**有效，进入执行期后调研者回归原红线；
- 交互轮次上限见需求3 的边界。

---

## 需求3：researcher 参考式拷问——降低用户反馈成本，区分三类用户

**1. 用户原话**

> 你们当前的research 子代里存在感很低，所以 你们可以 将主控的工作分到 research里面， 因为用户发送需求的时候自己可能也不知道 怎么干什么 想做成什么样，所以reseacher可以边搜索网上的模板边grill me用户（因为用户他可能是在某个地方/网站 看到了类似的东西，脑子里有画面但是不能描述出来，所以需要research根据用户的线索 去找一些成熟的模板啊，或者直接给用户research探索到的网址 直接说：用户你看是不是像这个网站里面的东西一样； 前低级了 太简陋了，所以你可以问用户你想要什么样东西然后给提供网址参考，也可以问用户说能不能提供 截图啊啥的 ，反正就是 让用户反馈消息太难->我们提供一些信息 让用户回复反馈更简单 得到用户的反馈就好办了；第三类就是用户直接提出的bug 自己直接理解了 那就可以了 ）

**2. 自己尝试理解（不一定正确）**

核心洞察：用户说不清想要什么时，让用户凭空回答开放题太难；应该反过来——researcher 先去网上搜成熟的模板/同类实现，把网址、参考样例、甚至要用户提供截图这些东西摆出来，让用户做"是不是像这个"的判断题，比让用户描述需求容易得多。

用户把来需求的人分成三类：
- 第一类：脑子里有画面但说不出来（曾在某地/某网站看到过类似东西，有线索）——researcher 根据线索去找成熟模板或直接给网址，让用户比对确认。
- 第二类：太简陋/低级，连线索都没有——researcher 先问用户大概想要什么样的，再根据回答去提供网址参考、要截图等，主动帮用户把需求具体化。
- 第三类：用户直接报 bug、需求自己说清楚了——不需要 researcher 介入，直接理解执行即可。

整体原则是"让用户反馈更简单"：提供信息让用户回复，而不是让用户从零生成信息。

用户认为当前 researcher 角色存在感太低，应该把主控在需求对齐阶段的工作分给它。

**3. 验收判据（可验证）**

- **必带参考物**：走特例的对齐问答记录里，至少出现一次具体参考物——可访问网址 / 成熟模板名 / 明确的截图索取；只有纯文字开放题 = 未达标；
- **提问形态**：核心问题以判断题或选择题呈现（"是不是像这个 / A 还是 B"），而非"你想要什么样的"这类空题；
- **网址真实性**：主控在转述前抽验至少 1 条网址可访问，避免编造链接；
- **三类走法各自成立**：
  - 第一类（有线索说不出）→ 按线索找模板/网址，让项目所有者比对确认；
  - 第二类（无线索）→ 先问粗粒度方向，再给参考物 / 索取截图收敛；
  - 第三类（自己说清了，如报 bug）→ **不启动 researcher**，主控直办（对应需求2 的分流）。

**4. 边界与约束**

- **轮次上限 ≤ 3 轮**：3 轮问答仍未收敛，转为"主控接手直接拍一个终态描述交项目所有者确认"或走原型通道（先做最小可看的东西再问），不许无限拷问——需求3 的目的是降低反馈成本，无限追问会反向抬高成本；
- 参考物只用于帮助表达，不构成技术选型决定（调研者不做决定的红线不变）；
- 依赖联网检索（拍板 3）。

---

## 需求4：researcher 产出文件控制——一次需求只产一个子集文件，合并后删除

**1. 用户原话**

> 2.至于文件，我们researcher 一次需求只能产出一个五行登记子集.md文件啊，并且合并之后删除就行了，你为什么说 《每条需求都产一份 researcher 登记文件》，难道你现在的五行登记 是每个需求一个文件？？ 别让我骂你！！

**2. 自己尝试理解（不一定正确）**

researcher 一次需求只允许产出一个文件（五行登记子集.md），不许一次需求衍生多个文件。这个子集文件给主控合并进总的需求登记后，就删除掉，不留中间产物。用户强调五行登记本身是一个文件里装多条需求的结构（每条需求占一节），不是每条需求一个文件——所以 researcher 的产出也应该是"一个子集文件装本次处理的若干条需求"，而不是每条需求单独一个文件。

用户对"每条需求产一份登记文件"的说法很不满，认为这是对现有五行登记制结构的误解。

**3. 验收判据（可验证）**

- **数量判据**：单次需求对齐，调研者新增文件恰好 1 个（文件名固定为需求登记子集类文件），出现第 2 个 = 红；
- **生命周期判据**：合并完成后该文件**不得在 HEAD 中持续存在**——判法：文件在 `git ls-files` 里且未被标记删除 = 红；
- **留证判据**：删除前必须已随某个 commit 入库（拍板 6），"从未入库直接删掉"= 销毁证据 = 红；
- 中文文件名下上述判据必须仍然有效（全局非功能需求·中文路径兼容）。

**4. 边界与约束**

- 该子集文件**同时是多轮对齐的状态载体**：子代理无跨轮记忆，第 2、3 轮追问的上下文靠读这个文件恢复；因此"一次需求一个文件"不只是省文件，也是状态收敛机制；
- 文件内一节一条需求（沿用五行登记结构），不是一条需求一个文件；
- 合并动作与删除动作应在同一个 commit 完成，避免出现"合并了但残留"的中间态。

---

## 需求5：文件爆炸——大项目中间文件数量必须控制

**1. 用户原话**

> 第二个建议：你测试过当用你们的项目开发一个大项目，需要产生多少中间文件吗？ 你们之前的一个版本，我就提了 20-30个需求，产生了 几百个 文档！！ 你知道有多 臃肿和浪费token吗？

> 2. 你自己看 之前用你的v4.0产生了多少文档！ 浪费了多少token，你的现在版本解决这个问题了吗？C:\Users\abyss\.qoderwork\workspace\mrw8rm7thm5qr0fl\mentor-ime\docs

**2. 自己尝试理解（不一定正确）**

用户用之前版本（v4.0）开发真实项目 mentor-ime 时，提了 20~30 个需求，结果在 docs 目录下产生了上百个文档（实测 134 个文件），极其臃肿、严重浪费 token。用户要求：项目必须对中间文件数量有真实有效的控制，不能让文件数量随需求数量线性甚至超线性膨胀。用户点名让对照那个历史项目目录看实际产生了多少，并质问当前版本是否真的解决了——要求是"实际解决"，不是文档里写一句建议。

（实测拆解 134 个文件：`audits/` 77 份 verdict + `decisions/` 17 份 ADR + `research/` 6 份调研报告 + 其余 34 份（其中 PLAYBOOK 被拆成 20 份分片）；`PROGRESS.md` 膨胀到 119KB。**四个增长源，缺一个都堵不住**——只压 verdict 不禁分片，文件数照样翻倍。）

**3. 验收判据（可验证）**

量化预算（按位置分别设限，回放上述历史项目应从 134 降到 ~26，其中 17 是有意保留的 ADR）：

| 位置 | 上限 | 理由 |
|---|---|---|
| `.aiops/docs/` **直属**文件（子目录不计） | ≤ 10 | 实测 init 后基线 3 个常驻 + 大需求期 6 份过程文档 = 9，留 1 格余量；**20 份 PLAYBOOK 分片机械上不可能** |
| `audits/` 新增文件 | 每个大需求 1 份台账（统一前缀），不再每批一份 | 77 → 数个，是最大单一增长源 |
| `research/` 在手文件 | ≤ 3，且大需求终验时必须清零（归档或删除） | 拍板 5；不设"活跃数 0"是因为执行期报告还要被读，强制立删等于逼人重查 |
| 需求登记子集 | 入库留证后**下一个 commit 必须删除**，不得在 HEAD 长驻 | 拍板 6 + 需求4 |
| `decisions/` | 不设上限 | 铁律6：ADR 永远留在 `decisions/`，设限会与之冲突 |
| `archive/<大需求名>-大需求/` | ≤ 12 | 归档也要收敛，否则只是搬家 |
| `PROGRESS.md` 行数 | 120 行黄牌（现有）+ **200 行硬红**（新增） | 只有黄牌，实测涨到 119KB；需要一条会真红的线 |

**判据本身的判据**：以上每一条都必须由 git 钩子在 commit 时执行并能真红；写在文档里靠自觉遵守 = 视同未实现（这正是 v4.0 翻车的原因）。

**4. 边界与约束**

- 不得为了压数字而违反铁律6（把 ADR 归档或合并）——预算与铁律冲突即为设计缺陷，须改预算而不是破铁律；
- 存量项目按"存量兼容口径"处理：首次触红归档一次，不倒查历史；
- 临时件不计入预算，但"入库留证"是前置条件（拍板 6）。

---

## 需求6：文件乱放——所有文件归到一个文件夹

**1. 用户原话**

> 3.你之前的版本呢 总是会将文件乱他妈的放，这次能不能放在一个文件夹了？

**2. 自己尝试理解（不一定正确）**

之前版本产出的文件散落在各处（根目录、docs 根下到处都是），没有统一归位。用户要求这次所有治理类文件集中放在一个文件夹下，不再乱放。结合需求5，用户要的是"既少又齐"——文件数量要控制，且存在的文件要归到一个明确的位置。

**3. 验收判据（可验证）**

- **根目录白名单**：目标项目根新增 `.md` 只允许 `AGENTS.md` 与 `README.md`，其余治理类文档一律落在治理目录内；违者 commit 时红；
- **机械拦截而非口头约定**：判据须由钩子执行；
- **对中文文件名有效**：本项目治理文件名以中文为主，判据不得因文件名非 ASCII 而静默放过（这是同类门禁最常见的失效方式）。

**4. 边界与约束**

- 宿主自己的目录（各家 AI 工具的 agents / 配置目录）不在本判据管辖范围，且不得在模板中硬写具体宿主目录名（拍板 1）——落盘位置用中性描述 + 宿主映射表表达；
- 已有散落文件按"存量兼容口径"处理，不倒查历史 commit。

---

## 需求7：每实现一个功能或批次，才能在对应地方交付回链（commit 与 verdict）

**1. 用户原话**

> 最后没实现一个功能或者批次才能在对应的地方交付回链——commit 与 verdict

**2. 自己尝试理解（不一定正确）**

交付回链（五行登记的第五行：commit 与 verdict）不是需求对齐阶段就写的，而是等真正实现了一个功能或一个批次之后，才在需求登记对应的位置补上交付回链。也就是说：需求登记先占节（①~④ 对齐阶段完成），⑤ 交付回链留空，等对应功能/批次实际做完、有 commit 和 verdict 了，才回填。这是对交付回链"何时写"的时序要求。

（注：这条与现有机制方向一致，用户可能是强调确认，也可能是要求把它作为 researcher 机制流程的一部分固化下来。）

**3. 验收判据（可验证）**

- **原话读法确认**：原话"最后没实现一个功能或者批次"按"**每**实现一个功能或者批次"理解（"没"为"每"的输入笔误）；语义即：先占位、后回填；
- **占位格式固定**：需求对齐完成时⑤栏不得留空，写成带状态的占位——`待交付回填 | 关联批次=<批号 | 未定>`；空白 = 红（空白无法区分"忘了写"和"还没做"）；
- **终验机械判据**：若⑤栏声明的关联批次在台账里已 PASS，而⑤栏仍处于 `待交付回填` 状态 → 终验 FAIL；
- 回填内容须同时含 commit 与 verdict 两个指针，缺一 = 未回填。

**4. 边界与约束**

- ⑤栏在需求对齐阶段**不许提前填**真实 commit/verdict（尚不存在，填了就是编造）；
- `关联批次=未定` 允许在对齐阶段存在，但一旦 PLAYBOOK 排定批次即须补实；
- 本判据只约束需求账本的⑤栏，不改变现行"提交必须携带验收结论"的既有门禁。

---

## 附：用户对前面"缺点回应"是否充分的确认

**1. 用户原话**

> 上面的足够回应你的 缺点了吗？

**2. 自己尝试理解（不一定正确）**

用户针对我上一轮提的几个缺点（researcher 直接对话 vs 通信架构冲突、全量过 researcher vs 免流程税、新增登记文件 vs 减文件、联网能力非所有工具都有）做了回应，认为自己已经用"主控分流路由 + researcher 特例放宽权限 + 一次需求一个子集文件合并后删除 + 五行登记不存在转述变味"把那些缺点都化解了，问我是否还认为有问题。用户要求我只列需求书、不解答。

**3. 评审回应（2026-09-04）**

| 原缺点 | 是否化解 | 依据 |
|---|---|---|
| researcher 直接对话与通信架构冲突 | **已化解，但换了办法** | 不开直连，改为主控逐字转发（拍板 2）——直连会被现行"冲突按更严格一份执行"的条款自锁，等于该例外永远用不上 |
| 全量过 researcher = 交流程税 | **已化解** | 分流制：需求明确的主控直办，只有说不清的才进 researcher（需求2） |
| 新增登记文件与"减文件"自相矛盾 | **已化解** | 子集文件定为临时件，入库留证后删除，活跃数 0，不占预算（需求4 + 拍板 5/6） |
| 联网能力不是所有工具都有 | **不化解，改为明确取舍** | 拍板 3 直接声明不支持无网/内网环境，接受这个适用范围收窄 |

**一处盘面事实需要更正**：项目所有者在答问中提到"调研报告主控拿完之后就会自动删除"——查现行条文并**不存在**这条自动删除规则，相反《智能体角色定义.md》要求 worker 打包提交时把 `research/` 等过程产物一并入库，归档说明也保留 `research/`；历史项目里实测残留 6 份。因此这个假设当前不成立，已通过拍板 5（临时件不计预算）与拍板 6（先入库留证再删除）新增"临时件生命周期"规则，让它在改造后成立。
